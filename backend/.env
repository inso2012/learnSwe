# configure the db connection using environment variables
DB_USER=postgres
DB_PASS=admin
DB_NAME=learnswe
DB_HOST=localhost
DB_PORT=5432

# JWT Secret for authentication (change this to a strong, random string in production)
JWT_SECRET=qweQWE123!@

# Server configuration
PORT=3000
NODE_ENV=development